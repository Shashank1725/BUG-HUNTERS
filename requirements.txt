# Core PDF parsing
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# DOCX parsing
python-docx>=1.1.0

# Excel and CSV parsing
pandas>=2.0.0
openpyxl>=3.1.0
tabulate>=0.9.0

# High-accuracy table extraction (optional, needs Ghostscript)
camelot-py[cv]>=0.11.0

# Fallback / messy-PDF parsing
unstructured[pdf]>=0.14.0

# Image captioning (BLIP)
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
Pillow>=10.0.0

# Utilities
reportlab>=4.0.0

# Person 2 — Relationship Modeling & Graph Construction
# -------------------------------------------------------
# Core (required)
networkx>=3.2

# Optional ML enrichment layers
sentence-transformers>=2.7   # MLRelationBuilder (CO_REFERENCE via cosine similarity)
spacy>=3.7                   # EntityCooccurrenceBuilder (named-entity co-occurrence)
# After installing spaCy: python -m spacy download en_core_web_sm

# ── Integration ─────────────────────────────────────────
# Person 1's schema.py must be on PYTHONPATH (or same folder).
# In the integrated repo rename schema_p1.py imports to:  from schema import ...

# Person 3 & Person 5 Integration Dependencies
qdrant-client>=1.18.0
pydantic-settings>=2.14.2
google-generativeai>=0.8.6
groq>=1.5.0
